using UnityEngine;
using System.Collections;

public class camer_Position : MonoBehaviour {
	
	// Use this for initialization
	void Start () 
	{
		transform.position = new Vector3 (0f, 0.8f, -5.35f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
